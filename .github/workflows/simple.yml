name: Shell Command

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a string
        run: echo hello world
      
      - name: multiline script
        run: |
          node -v
          npm -v
          
